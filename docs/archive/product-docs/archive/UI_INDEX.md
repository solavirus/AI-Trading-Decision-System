# UI Index

Updated At: 2026-08-08 00:50

"Status" tracks implementation progress (matches `DEV_STATUS.md` Section 2). "Frozen" tracks whether the page's purpose/responsibility is locked per `MVP Handbook.md` — these are independent axes; a page can be purpose-Frozen while its implementation Status is still In Progress.

Sidebar (not a "page" in the frozen 6-page workflow, but a persistent chrome element present on every page) now has one additional dev-only entry — "查看最近 Prompt（开发用）" — listed separately below the page table since it isn't part of MVP Handbook's page list.

| Page | Status | Frozen | Screenshot | Last Updated | Notes |
|---|---|---|---|---|---|
| Dashboard | Ready for Review | Yes | — | 2026-08-06 | 11/11 real data sources; purpose frozen per MVP Handbook |
| Research Input | In Progress | Yes | `research-empty-evidence.png` (partial — weights view, not the empty-evidence section itself) | 2026-08-08 | Purpose frozen; news-inspection stats line + read-only modal (Sprint 2.2); real Analysis Payload + real Prompt Record built on submission (Sprint 3.1A); the sidebar dev preview can now send that Prompt Record to a real backend AI Engine (Sprint 3.1B, no providers configured); full 11-source adapter still not displayed on this page itself. Sprint 3.1B.2: Additional Evidence starts empty (0 cards, was 4 fake ones) with click/drag/paste upload (image/PDF/TXT/Markdown, no auto-generated titles); Prompt no longer duplicates news headlines or all 6 assets' market/technical/derivatives data (real measured −36.1% prompt size). Sprint 3.2A built the Analysis Runner overlay as a UI-only mock; **Sprint 3.2B made it real** — clicking "开始 AI 分析" now performs real concurrent per-source data collection (fresh, never Dashboard's cache), real validation/timeout/cancel-on-failure, and real snapshot freeze, before building the same real Payload/Prompt Record as before (see the dedicated Analysis Runner row below). No longer auto-navigates to Strategy afterward — stops at "分析已就绪," since Strategy is still 100% mock and wiring a real Prompt into it isn't this sprint's job. |
| Strategy | Ready for Review | Yes | — | 2026-08-07 | Purpose frozen; AI output is currently **Mock** (deterministic signal model); does not consume the new Analysis Payload |
| Execution | Ready for Review | Yes | — | 2026-08-06 | Purpose frozen; mock fill/confirmation flow |
| Trade List | Ready for Review | Yes | — | 2026-08-06 | Purpose frozen; `localStorage`-persisted mock trades |
| Trade Detail | Ready for Review | Yes | — | 2026-08-06 | Purpose frozen; read-only, immutable per spec |
| Learning | Not Started | No | — | — | Disabled sidebar placeholder only; confirmed future work (`Quant Decision System.md`), no purpose defined yet |
| Settings / AI Connections | Ready for Review | N/A (not part of the frozen 6-page core workflow; scope frozen by its own sprint spec instead) | — | 2026-08-07 | Sprint 3.1B.1. Real sidebar route (`#/settings/ai-connections`), no longer a disabled placeholder. 5 provider cards (OpenAI/Anthropic/Google/DeepSeek/OpenAI-Compatible): enable toggle, base URL, API Key (masked, show/hide), default model, connection status, Test Connection, Save — all backed by real endpoints (`GET/POST /api/ai/config[/:provider[/test]]`). Scope explicitly excludes prompt editing, strategy generation, AI response viewing, learning, token statistics. |

`product-docs/screenshots/` has real, committed screenshots (first captured Sprint 3.2A, updated Sprint 3.2B to reflect the real flow) — see the Analysis Runner entry below and Research's row above. Every other page's Screenshot column above is still blank ("if none exists, leave blank") — those were only ever produced in ephemeral session temp storage, never committed.

**Analysis Runner overlay** (not a page — a full-screen overlay shown on top of Research; UI built Sprint 3.2A, real logic Sprint 3.2B): stages through Collect (one per weight>0 source, real concurrent fresh requests) → Process Evidence (real file-upload check) → Validate → Freeze Snapshot → Build Payload → Build Prompt, each with waiting/running/success/failed/skipped visual states and a live elapsed timer — no more 'AI' stage (deferred to Sprint 3.2C). Deliberately its own dark/monospace visual language, not the app's wizard-notebook theme, per explicit "should feel like Bloomberg Terminal" direction. Dev-only Diagnostics toggle (sidebar: 分析诊断信息) shows real collected/skipped/failed counts, collection duration, and Payload/Prompt sizes on the terminal state — replaces 3.2A's scripted success/failure/slow-network picker, which stopped meaning anything once collection became real. Ends with "分析已就绪 / Prompt 已成功构建" and [查看 Prompt（开发用）]/[关闭] — no more auto-navigate to Strategy. Screenshots: `analysis-runner-real-progress.png`, `analysis-runner-ready.png`, `analysis-runner-real-failed.png`, `sidebar-dev-review-mode.png`.

**Sidebar dev-only entry** (not a page): "查看最近 Prompt（开发用）" link-btn, next to "重置原型数据". Opens a modal showing the most recently built Prompt Record (Sprint 3.1A) — as of Sprint 3.1B, the same modal also has a provider/model picker and a "发送到 AI Engine" button that makes a real (currently always `provider_not_configured`, since no keys exist) backend request, with an attempt-history list below. As of Sprint 3.1B.2, the Model field auto-fills from the real, Settings-configured default model for whichever provider is selected (was a hardcoded per-provider guess). No screenshot committed yet.
