# PROJECT.md

This is the entry document for this project. Read this first — then read ONLY the one SPEC document relevant to the current task. Archived documents (`product-docs/archive/`) are historical only and should not be required to understand the current state.

---

# Project Overview

**Product goal**: build a decision workflow that helps users continuously improve their own trading decision model. This is not a trading terminal, not TradingView, not CoinGlass, not Bloomberg. The core asset is the Decision Weight Model, not market data — every trade is an experiment, and every experiment improves the user's decision model. (Source: `MVP Handbook.md`, `Quant Decision System.md` — both Frozen, both still authoritative for the product vision and the frozen 6-page core workflow; not duplicated here.)

**Current development phase**: past the original "mock-data-only prototype" stage named in `MVP Handbook.md`. **The full Research → AI → Strategy pipeline is now real and automatic** (Sprint 3.3C) — clicking "开始 AI 分析" on Research runs fresh data collection, image evidence parsing, Analysis Payload + Prompt Record creation, a real Main Analysis Model request (routed through the exact AI Connection configured for that role), automatic AI Response Parser validation, and — only once a valid `AIResponseRecord` exists — automatic navigation to a real Strategy page. No manual step is required; the dev-only Prompt Preview modal remains available for inspection/debugging but is no longer needed to complete an analysis. Settings manages multiple named AI Connections (Sprint 3.3B.2, `Settings → AI Connections`) with a separate Model Role layer (Main Analysis / Evidence Parser) pointing at whichever connection+model should do each job — the Main Analysis role is what the automatic flow now actually uses. Image Additional Evidence continues to be really parsed (Sprint 3.3B), including WHO appears to have posted a social-media screenshot (Sprint 3.3B.2), never a trading recommendation. Execution remains intentionally disconnected from real Strategy data (future sprint).

**High-level architecture**: one static frontend file (`prototype/index.html`, no bundler) talking to a local Node backend (`pipeline/`, no framework) over plain HTTP/JSON. See the pipeline below.

---

# Current Architecture

```
Dashboard
↓
Research
↓
Analysis Runner
↓
Analysis Payload
↓
Prompt Builder
↓
AI Engine
↓
AI Response Parser
↓
Strategy
↓
Execution
↓
Learning
```

Implemented end-to-end through **Strategy**. The Analysis Runner (Research's "开始 AI 分析") now automatically drives every arrow in this diagram in order: fresh collection → evidence parsing → Payload → Prompt Record → **AI Engine** (Sprint 3.3C: called automatically, via the Main Analysis Model Role, resolved to an exact AI Connection) → **AIRawResult** (persisted, multiple attempts traceable) → **AI Response Parser** (Sprint 3.2C, called automatically on the raw result) → **AIResponseRecord** (only created if validation passes) → **Strategy** (Sprint 3.3A's real render, auto-navigated to only after a valid record exists). A failure at any AI/Parser step cancels the Analysis with a readable message and never opens Strategy. **Execution** and **Learning** remain unwired to real Strategy data.

---

# Frozen Product Principles

Active decisions only — no historical discussion, no implementation detail (see the relevant SPEC or `product-docs/PRODUCT_STATUS.md` for those).

- Dashboard is a pure, independent real-time monitor — no AI, no strategy, raw data only.
- Research only configures inputs (source weights + additional evidence); it does not analyze.
- AI output appears only in Strategy.
- A data source with weight = 0 is excluded from collection and from the Analysis Payload entirely.
- Every Analysis Run collects fresh data only, after the user clicks Run Analysis — Dashboard's cache is never reused as Analysis data.
- If any required source fails, the whole Analysis cancels — no Payload, no Prompt Record, no silent fallback to cache.
- Every successful Analysis freezes exactly one immutable Snapshot, which becomes one Analysis Payload and one Prompt Record, both traceable by `analysisId`.
- Execution always separates the AI recommendation from the human decision, and only ever proceeds after explicit user confirmation.
- Trade Detail is read-only, immutable history — nothing is ever regenerated.
- All weights (data sources + additional evidence) must sum to 100%.

---

# Current Product Status

| Page | Status | Notes |
|---|---|---|
| Dashboard | Ready for Review | 11/11 real data sources live |
| Research Input | Ready for Review | Real Analysis Payload + Prompt Record via real Analysis Runner collection; image Additional Evidence is really parsed before the Payload freezes; the AI Engine, AI Response Parser, and Strategy navigation now all run automatically after "开始 AI 分析" (Sprint 3.3C) — no manual step required |
| Strategy | Ready for Review | Real — renders the v2 `AIResponseRecord` (`scenarioPlans`/`sourceAssessments`, Sprint 3.3D-B) for the current `analysisId`; reached automatically at the end of a normal Research run again (auto-navigation restored in 3.3D-B after 3.3D-A's temporary suspension). A stored major-1 (v1) record shows a clear "unsupported historical version" state, never adapted/guessed. The old deterministic mock generator (`generateStrategy()`) is unreachable dead code, kept only for Execution's shape compatibility — see below. As of Sprint 3.5D-B.1, every Scenario card shows an explicit "有效至" expiry timestamp (viewer-local time + UTC offset) and a "缺少可执行数值参数" indicator when a plan (PRIMARY or otherwise) isn't numerically execution-ready — 手动执行/触发时提醒 are disabled for it |
| Execution | Ready for Review | Real `ExecutionDraft` with `MANUAL` (Sprint 3.4A) and `NOTIFY` (Sprint 3.4B) execution modes, reached via "手动执行"/"触发时提醒" on any Strategy Scenario card, at `#/execution/:executionId`. `NOTIFY` runs a deterministic Trigger Monitor (`price`/`candle_close` only, no LLM) that promotes `PENDING`→`TRIGGERED`/`INVALIDATED`/`EXPIRED` and notifies in-app; confirmation is always `MANUAL`, never automatic. As of Sprint 3.4C, confirming a resolvable draft creates a real local `Trade`(`PLANNED`)+`Order`(`DRAFT`); an unresolved `ZONE`/`CONDITIONAL` entry blocks confirmation with a readable message instead of guessing a price. As of Sprint 3.5A/3.5C, confirmation also runs a deterministic, now **account-aware Risk Guard** (9 rules: 6 local + 3 real-exchange-aware) and shows PASS/BLOCKED + reasons right on the Execution page, with a "重新检查风险"/"返回策略重新执行" recovery path when blocked. As of Sprint 3.5D-B.1, a not-yet-confirmed (`DRAFT`) `ExecutionDraft` whose Scenario fails numeric execution readiness is blocked outright ("该方案不满足执行就绪要求" + specific reasons) — never an editable form with prose standing in for an order parameter; an already-`CONFIRMED` draft still renders as immutable history regardless. The old mock `Store.draft.strategy` flow remains isolated dead code. Sprint 3.5D-E adds a required, live-validated take-profit portion input per target (all portions must sum to exactly 100%) and a "完整交易计划" preview before confirming |
| Trade List | Ready for Review | Sprint 3.4C added real-v1 Trades (`qa_trades_real`); Sprint 3.5A added a small "风险：通过/阻止" badge per row. As of Sprint 3.6A.2, this is a real-only page — the legacy mock trade table/stat-strip/tabs (fake win rate, fake total PnL, "Sprint 3.4C 原型" label) was fully retired; the page shows only real Trades or an honest "还没有真实交易记录" empty state (no mock statistics shown as a placeholder — a real stats feature is future scope). Sprint 3.7 upgrades the row/status projection to `buildTradeListViewModel()` — one shared truth source (StrategyOrder + RealOrder + RealTrade + Position/Protection) instead of scattered `rt.status` inference, fixing a real reported contradiction (a StrategyOrder already `COMPLETED` via backend reconciliation still showing "持仓中"/"暂无真实成交" because only the possibly-stale browser-only `RealTrade.status` was read). Columns: 交易/来源/当前阶段/入场/持仓/结果/保护/创建时间/操作; Source is now explicit (手动执行/监控自动/AI 自主/未知·历史, never guessed); Result never fabricates a realized PnL for a closed externally-managed Trade ("PnL 待归因" instead). Filters remain explicitly deferred (not built) |
| Trade Detail | Ready for Review | Real-only routing as of Sprint 3.6A.2 (the legacy mock detail page, `renderTradeDetail()`, was fully deleted along with the mock `#/trades/:id` lookup — a stale/legacy id now shows the same honest "not found" state as any other unresolvable id). Sprint 3.4C adds a dedicated real-v1 detail view (`renderRealTradeDetail()`) resolving AI recommendation / user execution / Order / (absent) Position purely by ID; Sprint 3.5A/3.5C adds a Risk Guard section (status, checkedAt, account trace metadata, every rule's PASS/BLOCKED/SKIPPED + message, recheck action). Sprint 3.5D-A adds a real, strictly read-only **Order Lifecycle sync** — a manual "同步订单状态" button (only for an Order that already carries a real `exchangeOrderId`; a local-only `DRAFT` Order shows "尚未提交至交易所，无法同步订单状态" and no action) calls `GET /fapi/v1/order` via a backend proxy route, maps Binance's raw status to the local `RealOrder.status` enum, and truthfully propagates `SUBMITTED`/`PARTIALLY_FILLED`/`FILLED`/`CANCELLED`/`REJECTED`/`EXPIRED` to `RealTrade.status` (`OPEN`'s `openedAt` uses Binance's own fill timestamp, never a fabricated local one). Sprint 3.5D-B adds the first real **order submission**: a manual "提交到 TESTNET" button (TESTNET-only, ENTRY MARKET/LIMIT only, DRAFT-with-no-`exchangeOrderId` only) behind a mandatory confirmation modal, gated by a FRESH Risk Guard recheck immediately before the write. Sprint 3.5D-C adds the first real **Position**: a read-only local mirror (`RealPosition`, `qa_positions`) synced only from real exchange data via `syncPositionsFromExchange()` (manual "同步交易状态"/"同步本地镜像" buttons, no automatic polling); a linked `OPEN` Position renders the real `renderPositionCard()` (real quantity/entry/mark price/PnL/liquidation/margin/protection) with real `unrealizedPnl` in the header, a prominent no-real-stop-loss warning when `protection.stopLossOrders` is empty, and AI-planned protection shown in a clearly separate "AI 计划保护（尚未提交至交易所）" block — never merged with real exchange protection. Position↔Trade linking never guesses under ambiguity (stays unlinked, honest UI message). Sprint 3.5D-D adds the first real **order cancellation**: a "撤销 TESTNET 订单" button (only for `SUBMITTED`/`PARTIALLY_FILLED` with a real `exchangeOrderId`) that always runs a fresh pre-cancel exchange check before showing a mandatory confirmation modal (TESTNET, symbol, side, quantities, current status; explicit partial-fill warning); a truthful post-cancel summary distinguishes zero-fill ("交易计划已取消") from partial-fill ("已成交部分仍形成真实敞口，请查看持仓状态") outcomes; a passive notice appears when the Scenario has expired but the order is still live, without ever auto-cancelling it. Sprint 3.5D-E adds the first real **trade protection**: after a real fill, `reconcileTradeProtection()` automatically submits real Stop Loss (`STOP_MARKET`, 100% of protectable quantity) and Take Profit (`TAKE_PROFIT_MARKET`, one per portion target) orders via Binance's Algo Service (TESTNET only) — always based on real filled/position quantity, never planned position size; the "提交到 TESTNET" button is renamed "提交完整交易计划到 TESTNET" with an expanded confirmation modal showing the full plan; a new "保护状态" card shows each real protection order's trigger/portion/status/quantity, with an honest "保护状态未知" state when protection-read itself fails, plus a "重新建立保护" recovery button that only re-runs reconciliation, never resubmits the entry order |
| Settings / AI Connections | Ready for Review | Real connection-management UI (Sprint 3.3B.2) — supports multiple named connections, even of the same type; one real connection (migrated from the old `openai-compatible` provider config) is configured in this environment — see "AI Key Storage" below for the plaintext-on-disk review. Sprint 3.5A adds a compact local "风险守卫 Risk Guard" settings section (`qa_risk_settings`), extended in 3.5C with an explicit account-selection dropdown. Sprint 3.5B adds "交易所连接 Exchange Connections" — real, backend-persisted, **read-only** Binance USDT-M Futures connections (`pipeline/data/exchange_connections.json`, gitignored); supports LIVE/TESTNET, Test Connection, and a manual "查看账户"/"刷新账户" balance+position snapshot. Sprint 3.5C upgrades the account snapshot to full Futures position detail (margin, leverage, liquidation price, stop-loss/take-profit/trailing-stop protection). As of Sprint 3.5D-B, one real write capability exists — TESTNET-only entry order submission from Trade Detail (see below). Sprint 3.5D-C adds an optional "同步本地镜像" indicator/button next to the account snapshot. Sprint 3.5D-D adds the app's second real write, TESTNET-only entry order cancellation. Sprint 3.5D-E adds the third and fourth real writes, TESTNET-only Stop Loss/Take Profit protection order submission/cancellation via Binance's Algo Service. Sprint 3.6A adds the fifth real write, TESTNET-only leverage change — reachable only via an explicit recovery modal off a `leverage_mismatch` submission BLOCK, never a standalone "change leverage" button anywhere — still no amendment/margin-mode-mutation/transfer/withdrawal/Hedge-Mode-switch capability anywhere, and no LIVE write of any kind. Sprint 3.6A.2 adds explicit `ExchangeAccount` binding to the connection form ("所属交易所账户": NEW/EXISTING, never inferred) and a "首选连接"/"设为首选连接" control per connection — see EXECUTION_SPEC.md's "Exchange Account Identity" — plus a new "Data Management" card: real per-connection REST verification and a gated "确认清除本地交易历史" action (see "Trading History Reset" in EXECUTION_SPEC.md), replacing the retired mock "重置原型数据" button |
| Account / Portfolio | Ready for Review | New top-level page (Sprint 3.6A, `#/account`, distinct from Settings) — real account overview, every real Position (App+external undifferentiated in the list, each showing a "仓位归因" `PositionAllocation` breakdown — see EXECUTION_SPEC.md), every real ordinary open Order (App vs External ownership shown; manual cancel available for both, an extra-strong warning before cancelling an externally-created order), every real open Algo/Protection order (App vs External shown; manual cancel available as of Sprint 3.6A.2, reusing the pre-existing, pre-tested `DELETE /fapi/v1/algoOrder` capability from 3.5D-E — TESTNET-only, manual-confirm, same extra-strong warning pattern for External ownership — see "Exchange Order Management — Algo Cancel Reachability" in EXECUTION_SPEC.md). Real-time updates via a backend-only-credentialed SSE relay of Binance's User Data Stream (connection status pill + last-sync time shown honestly; REST "立即刷新" always available; a stream event only ever triggers a debounced REST reconciliation, never trusted alone). Position-management actions are exactly three read-only drill-downs (查看详情/查看保护/查看归因) — no ADD/REDUCE/CLOSE/REVERSE exists anywhere, not even as a placeholder |
| Learning | Not Started | Disabled sidebar placeholder only |

---

# Current Sprint

**Current Sprint**: 3.8C — Runtime Truth Foundation (implementation + tests complete; real TESTNET read-only verification passed; `runtimeVersion` → `r3.8c-1`). Answers "is the backend actually running as a real trading system independent of whether the browser is open?" — previously price data was pulled by the Monitoring Engine's own tick but account/position/order truth had **no** backend-owned continuous sync at all (only synced when an order was already in-flight, or when a frontend opened Account page's SSE). Adds two new backend modules, both pure callers of already-existing Binance read functions (`exchangeService.ts`) — no second Binance client: **`exchange/exchangeRuntimeSync.ts`** — in-memory (never persisted; Runtime State vs Business Records), per-`ExchangeAccount` sync starting immediately at process boot and then every `SYNC_INTERVAL_MS` (**10s** as of the 3.8C Closeout, was 20s at initial 3.8C ship), tracking `syncHealth` (`STARTING`/`HEALTHY`/`DEGRADED` at 3 missed cycles/`ERROR` at 9 missed cycles/`DISABLED` for a structurally-unresolvable `preferredConnectionId` — never attempts a network call for the latter, never fallback-substitutes a different connection); a failed sync **never** fabricates an empty account (last known-good `snapshot`/`openOrders` are preserved verbatim, only `syncHealth`/`lastError` change). **`strategy/executionReadiness.ts`** — computes `StrategyOrder.executionReadiness` (`READY`/`BLOCKED`/`DEGRADED`/`UNKNOWN` + machine-readable `reasonCode` + real `userMessage`) PROACTIVELY every Monitoring Engine tick for every `ARMED` order (not only at trigger time), so a broken `ExchangeAccount`→`ExchangeConnection` binding surfaces to the user *before* the price condition is ever met — this is diagnosis only, never a bypass: the real submission chain (`attemptAutoSubmission()` — Kill Switch → Execution Guard → connection resolution → AUTO Risk Policy → Risk Guard → TESTNET submission) is completely unmodified and still reruns in full at actual trigger time regardless of what Readiness last reported. New `GET /api/exchange/runtime` route (one backend-computed truth, frontend never stitches multiple endpoints itself); `GET /api/strategy/orders` gained the `executionReadiness` field non-destructively. Frontend truth-wiring only (no page redesign): `renderStrategyOrderCard()`'s "下一步" text for `ARMED` no longer unconditionally claims "系统自动监控中" — it now derives STARTING/RUNNING/STALE from real `monitoringState.lastCheckedAt`/`lastObservedDataAt` freshness (45s = 3×15s monitor-tick threshold, deliberately a *different* constant from the Exchange Runtime Sync one — the two freshness policies are for different subsystems and were kept independently named on purpose); a new "执行能力" row shows `executionReadiness.userMessage` directly, never re-derived client-side. A real, pre-existing production bug was found (not fixed automatically, per explicit instruction) and is now the sprint's own live verification sample: a real `BTC/USDT SHORT` `StrategyOrder`'s bound `ExchangeAccount.preferredConnectionId` points at a deleted `ExchangeConnection` — Execution Readiness correctly reports `BLOCKED`/`CONNECTION_MISSING` **11+ minutes before** the order's real price trigger later fired and was — exactly as designed — safely caught at the existing `attemptAutoSubmission()` connection-resolution gate (`EXECUTION_BLOCKED`, zero Binance write); the order was never modified, rebound, cancelled, or auto-submitted. Real TESTNET verification (read-only, backend running with frontend closed throughout): observed `lastAttemptAt`/`lastSuccessfulSyncAt` self-advance across multiple sync cycles and `monitoringState.lastCheckedAt` self-advance across multiple monitor ticks, cross-checked account truth against a direct `GET /api/exchange/connections/:id/account` read (identical), zero Binance writes. Rate cost audited and disclosed: one valid flat (no open position) account costs ~3 real Binance REST reads per sync cycle (`/fapi/v2/account` + `/fapi/v2/positionRisk` + `/fapi/v1/openOrders`) → ~18 requests/minute/account at the 10s interval (scales by +1 request/cycle per additional symbol with an open position) — not evaluated as excessive for this minute-scale strategy system. **12 + 17 + 13 = 42 new backend/frontend tests** (Execution Readiness state matrix, account isolation, no-fallback, and — added in the Closeout specifically via a minimal fetch-function DI seam in `exchangeRuntimeSync.ts` — full failure/DEGRADED/ERROR/recovery truth-boundary coverage without any real network dependency), plus the full existing Sprint 3.8 frontend regression (41+20 tests) reconfirmed unaffected. `StrategyOrder`/`monitorEngine.ts`/Risk Guard/Execution Guard/AUTO Risk Policy/Kill Switch/Manual Analysis/Autonomous Analysis logic itself: **zero changes** (only `monitorEngine.ts`'s existing `resolvePreferredConnectionForAccount()` helper relocated verbatim to `exchange/exchangeAccountStore.ts`, re-exported for `autonomousScheduler.ts`'s unchanged import). 3 known orphan `ExchangeAccount`s (including the one above) disclosed, not cleaned up — deferred to a future, separately-authorized Data Integrity Cleanup. `EXECUTION_SPEC.md` not updated (no write-capability/contract semantics changed, only new read-side runtime-truth surfacing).
**Previous Sprint**: 3.8 — UX Convergence / Product Simplification (implementation + targeted tests complete; frontend-only, `prototype/index.html`; zero backend files touched — BACKEND FREEZE fully honored, no exception needed). Reorganizes the existing, already-built Sprint 3.6/3.7 capabilities into one converging user flow (首页→开始分析→分析结果→策略→监控/交易→交易列表) without changing any backend state machine, write chain, or safety boundary. Key changes: **Homepage** gained a compact "开始分析" Analysis Entry card (BTC/USDT price, live `X/11 数据源已更新` freshness count, and — reading only the already-persisted `AIResponseRecord` map, never a new AI call — either a "最近一次分析" summary linking to `#/strategy` or an honest "暂无分析结果" empty state); the old "＋ 新建交易实验" CTA is now "开始分析" (same `new-trade` action, "交易实验" no longer used as a concept). **Analysis Result** (`renderStrategyReal`) now labels `decision.action` as 建议执行/等待机会 instead of raw "ENTER"/"WAIT". **Strategy cards** (`renderScenarioPlanCard`) show "等待条件"/"满足以后" for a WAIT plan and "现在执行" for an ENTER/OPEN plan, reusing the already-computed entry text. **Monitoring** reorganized from a two-bucket 监控中/历史 split into three user-meaning buckets — 等待中的策略 / 执行中的策略 / 历史; `STRATEGY_STATUS_LABEL` relabeled per an explicit copy-mapping table (ARMED→等待条件, TRIGGERED→条件已满足, ENTRY_SUBMITTED→订单已提交, EXECUTION_BLOCKED→暂时无法执行); the Autonomous card is now "自动分析" (not "AUTO Control Center"/"自主交易"), with a capability checklist (✓自动分析 ✓自动生成等待策略, plus a conditional ✓/✕ for direct execution) and an explicit "自动分析只是：系统定时替你执行一次平时手动点击的分析流程" explainer; Kill Switch copy is "自动执行中"/"暂停自动执行" (no more literal "AUTO Kill Switch" header); a real autonomous NO_ACTION cycle now renders as "当前不建议交易" + real rationale + [返回首页]/[重新分析], never styled like a failed/error cycle. **Trade List/Detail**: `TRADE_SOURCE_LABEL` relabeled 手动/监控策略/自动分析 (was 手动执行/监控自动/AI 自主); Trade Detail's raw `analysisId`/`scenarioPlanId`/`executionId` moved into a collapsed `<details>` "高级详情 / 审计日志" block at the bottom (native disclosure widget — this codebase had no prior collapsible pattern); the already-existing (Sprint 3.7) `buildTradeListViewModel()` truth-priority projection and its "PnL 待归因" honest-non-fabrication behavior were verified still correct, not re-architected. **Responsive**: added narrow-width (`≤700px`/`≤640px`) single-column fallbacks for `.g2/.g3/.g4/.g6` grids and the Homepage source-card columns (not a full mobile pass — out of scope this sprint). No StrategyOrder/monitorEngine/autonomousScheduler/RiskGuard/KillSwitch/Position/Allocation/Protection backend logic was touched; only one small, disclosed frontend logic fix was needed (`openAnalysisResult()` rehydrates `Store.lastAnalysisPayload` from the already-persisted `AnalysisPayload` before navigating to `#/strategy`, since that field was previously in-memory-only and would otherwise make "查看分析结果" from a past analysis show "策略不可用" after any page reload). **41/41 targeted frontend tests passed** (vm-sandbox slice of `prototype/index.html`, zero real network calls asserted); full-script syntax check clean. `PROJECT.md` updated; `EXECUTION_SPEC.md` not updated (no backend/contract semantics changed, only display labels).
**Previous Sprint**: 3.7 — Autonomous Trading V1 + Trade List Upgrade (implementation + tests complete; no real Binance/AI-provider interaction occurred during coding/testing). Builds on the real-TESTNET-verified 3.6B/3.6B.1/3.6B.2/3.6B.2.1 chain by adding a third execution mode, `AUTONOMOUS`: a backend-owned scheduler (`autonomousScheduler.ts`) periodically (default every 4h, configurable) runs a token-conscious Pre-AI Gate (enabled/Kill Switch/allowed-symbol/TESTNET-only/account-readable/no-existing-Position/no-existing-active-Strategy/fresh-market-data — any failure costs zero AI calls), then calls the AI via the EXISTING `runAIAnalysis()` primitive with a new, deliberately narrow, strictly machine-readable decision contract (`NO_ACTION`/`WAIT`/`OPEN` only — never a reuse or redesign of the frontend `AI_RESPONSE_SPEC.md`/`ScenarioPlan` pipeline, which stays completely untouched), validates the response deterministically (never guesses a missing/fuzzy field), and hands a validated `WAIT`/`OPEN` to the fully unmodified StrategyOrder/Monitor Engine/Execution Guard/AUTO Risk Policy/Risk Guard/Kill Switch/TESTNET-only chain — the AI never talks to Binance directly and can never bypass a gate. Idempotency is structural: `AutonomousCycle.cycleId` is deterministic from `symbol+scheduledAt`, so a restart or duplicate tick can never re-run or duplicate a slot. A decision that's valid but not currently permitted (wrong direction, or OPEN when disabled) still produces a durable, auditable `EXECUTION_BLOCKED` StrategyOrder, never a silent drop. New account-level `AutonomousPolicy` (separate from the existing `AutoRiskPolicy`, which continues applying unweakened to every AUTONOMOUS submission) and a compact "AUTO Control Center" card added to the existing Monitoring page (health/schedule/recent AI decisions/enable-disable/Run-Analysis-Now — all still fully gated, never a schedule-bypass shortcut). Also fixes a disclosed 3.6B.2.1 UI imprecision (orphan count/detail now reads a new precise `activeOrphanProtectionOrderIds` field instead of the full historical `protectionOrders` array) and upgrades Trade List to a single shared `buildTradeListViewModel()` projection (see the Trade List row above), fixing the exact reported contradiction of a backend-`COMPLETED` StrategyOrder still showing "持仓中"/"暂无真实成交" via a stale browser-only `RealTrade.status` read. No sixth real write function — `AUTONOMOUS` reuses the identical `attemptAutoSubmission()`/five-write-function chain `MONITORED_AUTO` already uses. `AUTO REDUCE/CLOSE/REVERSE`, LIVE autonomous trading, AI-controlled leverage/SL/TP mutation after a Position is active, and Trade List filters remain explicit non-goals/deferrals. **98 new tests** (67 backend + 31 frontend), full regression clean (648 backend across 17 suites + all 23 frontend suites; two pre-existing assertions disclosed-intentionally updated to match this sprint's own genuine wording/precision changes). `EXECUTION_SPEC.md` → 1.14.0. Coding-phase safety: zero real Binance reads/writes, zero real AI provider calls — `ai/connectionStore.ts`/`ai/configStore.ts` gained test-path isolation overrides specifically so this sprint's own tests could never risk the user's real configured AI connection. Per the spec's own Section 33/55, real TESTNET autonomous verification was NOT performed this sprint — waiting for separate review/approval.
**Previous Sprint**: 3.6B.2.1 — Orphaned Protection Reconciliation Hotfix (implementation + tests complete; no real Binance read/write occurred during coding/testing). Closes a real gap 3.6B.2's own real, read-only TESTNET verification found live: after the real BTCUSDT position from 3.6B.1 was manually closed, the StrategyOrder correctly completed (`COMPLETED`/`completionSource:'EXTERNAL_POSITION_CLOSE'`), but its two real app-owned protection orders (Stop Loss/Take Profit) — independently confirmed still genuinely live on Binance (`algoStatus:'NEW'`) via two separate direct reads — had been recorded locally as "clean, no orphan," because a single close-time read was allowed to permanently finalize that classification with no way to re-check once `status` left `POSITION_ACTIVE`. Adds a frozen rule: **External Position close completion and protection cleanup state are separate truths** — `execution.protectionStatus` is now frozen the instant `status` becomes `COMPLETED` (never repurposed to describe current truth), and a new, independent field, `execution.postCloseProtectionState` (`null|PENDING|ORPHANED_PROTECTION|CONFIRMED_NONE|READ_FAILED`), tracks current-after-close protection truth via a new `reconcileClosedPositionProtection()` — running every tick and at restart recovery for any `COMPLETED`-via-`EXTERNAL_POSITION_CLOSE` order not yet terminal, requiring **two consecutive independent clean reads** before ever finalizing `CONFIRMED_NONE` (one transient/empty read is never final truth), and self-healing back toward `CONFIRMED_NONE` with no local edit and no code change if the orphan is later cleared by any means (including a user manually cancelling it directly on Binance). Ownership matching unchanged — only this StrategyOrder's own previously-attempted `exchangeOrderId`s ever count as an App orphan. Monitoring card and Trade Detail now render "current" and "historical" protection from the two separate backend fields, never one field read two ways, and never claim "当前无活动保护" before a real read has confirmed it. No automatic cancellation — detect/persist/display/self-heal only, same as 3.6B.2. **41 new tests** (24 backend + 17 frontend), full regression clean (581 backend across 15 suites + all 22 frontend suites, one pre-existing 3.6B.2 assertion disclosed-intentionally updated to reflect the new two-field model — a stale Windows test-directory-cleanup flake in `__test_data_3_6b_2` was also found and disclosed, root-caused to `rmSync` not reliably firing across rapid-succession `tsx` subprocess runs, not a product bug). `EXECUTION_SPEC.md` → 1.13.0.
**Previous Sprint**: 3.6B.2 — External Position Closure Reconciliation + Trade Navigation Hotfix (implementation + tests complete; no real Binance read/write occurred this sprint). Closes gaps the real 3.6B.1 TESTNET verification directly surfaced: after that verification's real BTCUSDT position was manually closed on Binance, the app kept showing it as "持仓中"/"已进入持仓管理" forever, Trade List showed an internally-contradictory row ("已成交" order + "尚未建立仓位" + "持仓中" all at once), and neither Trade List nor Monitoring's "关联交易 → 查看" link could open Trade Detail at all (a real crash: `renderRealTradeDetail()` unconditionally indexed `rt.executionIds[0]`, a field that only exists for a Trade created through an `ExecutionDraft` — every `MONITORED_AUTO`-originated Trade has none). Adds a frozen rule: **External Exchange Truth may complete a local Trade lifecycle** — a full external Position close (confirmed via real exchange read only, never Strategy expiry/a browser click/stale local state) now closes the `RealPosition` lifecycle (already true since 3.5D-C), completes every linked `RealTrade` (`closeSource:'EXTERNAL'`, new) via the existing `PositionAllocation` model, and completes the linked `StrategyOrder` (`COMPLETED`/`completionSource:'EXTERNAL_POSITION_CLOSE'`, new — `monitorEngine.ts` now keeps re-checking a `POSITION_ACTIVE` Strategy's real position quantity every tick forever, exactly like 3.6B.1's other post-submission steps, so this works fully headless too). Never fabricates exit price/realized PnL/exit-order attribution; a partial external reduction never completes anything (stays a `CONFLICT` per the existing 3.6A rule); multiple Trades sharing one Position complete together, never arbitrarily one. An app-owned SL/TP still live after its Position closes is detected (via a new symbol-explicit protective-order read, since the account snapshot's own read is — correctly, for its own use case — scoped to non-zero positions and therefore structurally blind to this exact case) and surfaced as an honest "遗留保护单" warning, never auto-cancelled. Also fixes: a real `StrategyOrder.failureReason` staleness bug (an old block reason kept displaying as if still current after a later successful retry — now cleared at the start of every fresh trigger attempt and on resume, historical blocks remain in the audit log); the real AUTO Risk Policy symbol-format mismatch the 3.6B.1 verification hit live (`BTC/USDT` vs `BTCUSDT`, fixed via canonical-symbol comparison, never fuzzy matching); a Monitoring card that kept showing a "取消" button for an already-filled/completed Strategy. No `ExecutionDraft`/`RealOrder`/`RealPosition`/`PositionAllocation` field renamed or reinterpreted. No new low-level Binance write capability — orphan detection is a new caller of the existing read-only `GET /fapi/v1/openAlgoOrders`. `AUTO CLOSE/REDUCE/REVERSE` remain explicit non-goals — this sprint only reconciles a closure that already happened by some other means, never initiates one. **59 new tests** (29 backend + 30 frontend), full regression clean (553 backend + all 24 frontend suites, zero disclosed fixture updates needed). `EXECUTION_SPEC.md` → 1.12.0. This sprint's own real, read-only TESTNET verification (run 2026-08-12) found the real gap 3.6B.2.1 (above) fixes — see that entry.
**Previous Sprint**: 3.6B.1 — Headless AUTO Safety Closure (implementation + tests complete; no real Binance read/write occurred this sprint). Closes two safety gaps 3.6B itself disclosed at the end of that sprint. **(1) Headless lifecycle**: previously, a `StrategyOrder`'s post-entry safety-critical steps (fill detection, Position sync, `PositionAllocation`, Stop Loss/Take Profit protection, expiry-driven remainder cancellation) only completed once the frontend happened to load `#/monitoring`/`#/trades` and ran `reconcileAutoSubmittedStrategyOrders()`. Now the backend Monitoring Engine tick (`monitorEngine.ts#progressStrategyOrderPostSubmission`, plus `recoverOnStartup()`) completes the ENTIRE chain alone — real order-fill reconciliation, real Position sync, allocation from confirmed fill only, protection creation (new-missing-only) with a real read-back verification before ever reporting `PROTECTED`, and safe remainder cancellation on partial-fill expiry — with zero browser tabs open at any point. A new backend-only `StrategyOrder.execution` sub-object is the authoritative confirmed-real-state record for this; the existing frontend `RealOrder`/`RealTrade`/`RealPosition`/`PositionAllocation` chain is completely untouched/frozen and becomes a pure downstream display mirror (`reconcileAutoSubmittedStrategyOrders()` is now read/display-only, no longer an independent protection decision-maker). **(2) AUTO Risk Policy**: 3.6B's backend Risk Guard rerun could not enforce `maxNotionalPerTrade`/`maxLeverage`/`maxActiveTrades` at all, because those settings lived only in browser `localStorage`. Adds a NEW, minimal, backend-persisted AUTO Risk Policy (`pipeline/data/strategy_auto_risk_policy.json`, TESTNET-only, `GET`/`PUT /api/strategy/auto-risk-policy`) — deliberately not a migration of the manual Risk Guard settings — evaluated alongside (never replacing) the existing Risk Guard/Execution Guard/Kill Switch checks at trigger time; fails closed by default (`enabled:false`). Also fixes a real Capability Router bug found during the required audit: `EXCHANGE_NATIVE` now requires the trigger's price level AND direction to match the LIMIT entry's natural fill behavior, not merely "single price condition + LIMIT entry" — see `docs/BINANCE_CAPABILITY_MATRIX.md`. No `ExecutionDraft`/`RealOrder`/`RealTrade`/`RealPosition`/`PositionAllocation` field renamed, removed, or reinterpreted. No sixth real write function — headless protection reuses the existing `POST`/`DELETE /fapi/v1/algoOrder`, remainder cancellation reuses the existing `DELETE /fapi/v1/order`. **47 new targeted tests**, full regression clean (477+47 backend, all frontend suites — two pre-existing 3.6B test assertions disclosed-intentionally updated: one capability-router fixture that exercised the exact misclassification this sprint's audit fixed, one restart-idempotency assertion that predated the new headless re-read-on-restart behavior). `EXECUTION_SPEC.md` → 1.11.0.
**Previous Sprint**: 3.6B — Strategy Order Management + Trigger Engine + Armed Auto Execution (implementation + tests complete; no real Binance read/write occurred this sprint). Introduces `StrategyOrder`, a new backend-persisted layer between the immutable AI `ScenarioPlan` and the frozen `ExecutionDraft`/`RealOrder`/`RealTrade` chain (none of which were redesigned), with a real state machine (`READY`/`WAITING_TRIGGER`/`ARMED`/`PAUSED`/`TRIGGERED`/`EXECUTION_BLOCKED`/`ENTRY_SUBMITTED`/`PARTIALLY_FILLED`/`POSITION_ACTIVE`/`EXPIRED`/`CANCELLED`/`COMPLETED`/`ERROR`) replacing the ambiguous "计划中" label wherever a Trade has a linked StrategyOrder. Adds a deterministic backend Trigger Engine + Capability Router (`EXCHANGE_NATIVE`/`APP_MONITORED`/`UNSUPPORTED`, see `docs/BINANCE_CAPABILITY_MATRIX.md`), a real "监控待执行" Monitoring page (`#/monitoring`, backend-owned tick loop + SSE push, never a browser timer as the source of truth), and the app's first user-authorized automatic entry capability (`MONITORED_AUTO`) — full-disclosure explicit authorization, Execution Guard + a backend-ported Risk Guard rerun at trigger time, TESTNET-only with a hard LIVE block even when authorized, and a system-level AUTO Kill Switch (defaults PAUSED). No sixth write function — automatic submission reuses the existing entry-order pipeline verbatim; a real auto-submitted order becomes a genuine local `RealOrder`/`RealTrade` only via the existing, unchanged reconciliation pipeline. Crash/restart idempotency verified via `pendingClientOrderId`-before-write + real-read recovery, matching the existing manual-submission discipline. **114 new tests** (78 backend + 36 frontend), full regression clean (477 backend + all frontend suites, zero disclosed-intentional fixture updates needed). New `docs/ARCHITECTURE_INDEX.md` and `docs/BINANCE_CAPABILITY_MATRIX.md`. `EXECUTION_SPEC.md` → 1.10.0.
**Previous Sprint**: 3.6A.2 — Exchange Account Identity + Trading Data Cleanup + Real Account Operations (implementation + tests complete; no real Binance read or write occurred this sprint). Three interdependent parts, implemented in order with targeted tests between each: **(A)** adds `ExchangeAccount` — a local-only ownership identity trading data now attributes to (Binance has no reusable stable account field in this app's real environment, confirmed by live research), with `ExchangeConnection` narrowing to "historical execution channel"; an API-key rotation (deleting Connection A, binding Connection B to the same account) no longer orphans a Trade's order sync/protection reconciliation, and `RealPosition`/`PositionAllocation` identity/dedup upgrades to account-scoped without breaking 3.6A.1's lifecycle rules. **(B)** retires all mock/prototype trade UI (stat strip, table, detail page, "重置原型数据") from the Trade List/Detail pages — real Trade data only, no mock statistics shown as a placeholder. **(C)** adds a real, REST-verified Local Trading History Reset (`Settings → Data Management`) that refuses to run unless a fresh check proves every enabled connection's account is genuinely flat (zero positions/orders/algo orders), and never makes a Binance write; also wires the Account page's pre-existing, pre-tested `DELETE /fapi/v1/algoOrder` capability (3.5D-E, previously unreachable from any UI) to a manual-confirm Cancel button — a UI-reachability change, not a new write function. The five real write functions remain unchanged in number and shape; Manual Position Close remains explicitly out of scope, deferred to a future dedicated sprint. **85 new tests** (18 backend + 20+10+27 frontend across the three parts), full regression clean (379 backend + all frontend suites, two pre-existing frontend assertions disclosed-intentionally updated to match this sprint's own copy/UI changes). `EXECUTION_SPEC.md` → 1.9.0.
**Previous Sprint**: 3.6A.1 — Position Allocation Lifecycle Hotfix (implementation + tests complete; no real Binance write occurred this sprint). Fixes a real gap in 3.6A's `PositionAllocation`: `RealPosition.positionId` can be revived when a fully-closed position reopens on the same symbol (or, in One-way Mode, even after a direction reversal), so a historical Trade's ENTRY fill was at risk of being silently re-attributed to a position it never actually contributed to. Adds `RealPosition.positionLifecycleId`/`PositionAllocation.positionLifecycleId` (additive, backward compatible) plus a global "an ENTRY fill is consumed by at most one allocation, ever" rule. 29 new tests (Test 1–7 from the hotfix spec plus root-cause reproduction and a no-spurious-lifecycle-churn regression guard), all passing; full regression clean, zero fixture updates needed. A related, structurally-identical staleness in `linkPositionToTrade()`'s `sourceTradeIds` was found and disclosed but deliberately left unfixed (out of this hotfix's stated scope). `EXECUTION_SPEC.md` → 1.8.0.
**Previous Sprint**: 3.6A — Position Allocation + Account Management Foundation (implementation + tests complete; no real Binance write occurred this sprint)
**Previous Milestone (3.5D-E.1, 2026-08-10)**: the real, manually-approved TESTNET full-trade-plan verification 3.5D-E asked for was completed successfully — one real LIMIT entry order (exchangeOrderId `28536100974`) FILLED at 65250 for 0.0153 BTC, independently cross-verified against a real `GET /fapi/v1/order` read; a real BTCUSDT LONG Position was confirmed (0.3893 total, correctly composed of this trade's own 0.0153 contribution plus a separate 0.374 pre-existing contribution from an earlier session); real Stop Loss (64300) and both real Take Profit targets (66250/67500, 50%/50%) were confirmed active via a real read-only `openAlgoOrders`-equivalent query, correctly scoped to only this trade's own 0.0153 contribution; a repeated "同步交易状态" produced no duplicate protection and no second Entry. Protection Status = PROTECTED. No code was changed, no real bug was found. This is exactly the "one net Position, multiple contributions" scenario Sprint 3.6A's `PositionAllocation` model (below) formalizes.
**Previous Sprints (3.3C → 3.5D-E, complete)**: 3.3D-A/B/C built Strategy Decision Model v2.1. **3.4A** built `ExecutionDraft`+`MANUAL`. **3.4B** added `NOTIFY`'s deterministic Trigger Monitor. **3.4C** implemented `Order`+`Trade` (real-v1). **3.5A** added a deterministic local **Risk Guard**. **3.5B** built the first real, strictly read-only Binance USDT-M Futures exchange connection. **3.5B.1** was a hotfix. **3.5C** upgraded the account layer to full Futures position/risk detail and made Risk Guard account-aware. **3.5C.1**/**3.5C.2** fixed real TESTNET timestamp/hang bugs. **3.5D-A** built a strictly read-only Order Lifecycle sync layer. **3.5D-B** implemented the app's first real exchange WRITE — `POST /fapi/v1/order`, TESTNET-only, entry orders (MARKET/LIMIT) only, behind a mandatory confirmation modal + fresh pre-submission Risk Guard recheck + a three-layer TESTNET hard guard + non-retrying network-ambiguity handling (`pendingClientOrderId` recovery via `origClientOrderId`). **3.5D-B.1** closed a Parser/UI gap so only a numerically-complete `PRIMARY` plan can reach TESTNET eligibility (`AI_RESPONSE_SPEC.md` → 2.2.0, `evaluateScenarioExecutionReadiness()`). **3.5D-C** formally defined `Position` (`RealPosition`, `qa_positions`) as a read-only mirror synced exclusively from real exchange data. Two narrow hotfixes followed 3.5D-C: a Binance Futures **symbol-normalization** fix (app-canonical `"BTC/USDT"` was never converted to Binance's own `"BTCUSDT"` before being sent to `exchangeInfo`/`positionRisk`/order endpoints — fixed at the adapter boundary via `toBinanceFuturesSymbol()`) and a **test-storage isolation** fix (an old test file's unmocked `createExchangeConnection()` call had, when run from the wrong cwd, written a synthetic `"ValidConn"` connection into real production storage — root-caused, the stray connection safely deleted after being proven test-fixture-only, and `exchangeConnectionStore.ts` given an explicit, production-default-unchanged test-storage-path override so this class of pollution can't recur). Neither hotfix touched `ExecutionDraft`/`RealOrder`/`RealTrade`/`RealPosition` shape or weakened any safety boundary. **3.5D-D** implemented the app's second real write, TESTNET-only manual entry-order cancellation (`DELETE /fapi/v1/order`) — a real user cancellation attempt afterward failed due to a stale backend process (diagnosed and restarted; no code defect). **3.5D-E** implemented the app's third and fourth real writes, TESTNET-only Stop Loss/Take Profit protection order submission and cancellation via Binance's Algo Service (`POST`/`DELETE /fapi/v1/algoOrder`) — verified for real end-to-end in **3.5D-E.1** (summarized above). Full detail on all of the above is in `EXECUTION_SPEC.md`, not repeated here.

**Previous Milestone (3.5D-B.1)**: closed a real product gap surfaced while preparing for that first TESTNET order — a `PRIMARY` `ScenarioPlan` could previously be entirely prose (`entry.type="CONDITIONAL"`, description-only `stopLoss`/`takeProfit`, no `expiresAt`) and still reach Execution/TESTNET-eligibility once a user manually typed numbers into the override fields. Freezes: **Strategy explains WHY, Scenario quantifies WHAT/WHEN/WHERE, Execution executes numbers.** No TESTNET order was submitted this sprint — this is a Parser/Prompt/UI-readiness tightening only.
- **`AI_RESPONSE_SPEC.md` → 2.2.0** (additive tightening, same precedent as 2.0.0→2.1.0): `entry.type="CONDITIONAL"` removed as a valid final parameter for the `PRIMARY` plan (the concept moves into `triggerStructured`, `entry` itself must be a numeric `MARKET`(ENTER-only, no `value`)/`LIMIT`/`ZONE`); `stopLoss.value`/`takeProfit[].value` now required real numbers for `PRIMARY` (description alone no longer suffices there); `reviewHorizon.expiresAt` now required for `PRIMARY` — must be an absolute, timezone/offset-qualified ISO-8601 timestamp, strictly after generation time; `triggerStructured` now required+machine-evaluable for a `WAIT`-decision `PRIMARY` specifically; a new conservative directional price-sanity cross-rule (LONG: stop below/TP above entry reference; SHORT: reverse; skipped for `MARKET`, which has no reference price). `ALTERNATIVE`/`INVALIDATION` plans keep 2.1.0's Parser-level completeness rule (description-or-value still valid there) — historical 2.0.0/2.1.0 records remain stored as-is, never migrated/re-validated.
- **Prompt** (`PROMPT_VERSION` → 2.2.0): Output Contract section rewritten with the new ENTRY/STOP-TAKE-PROFIT/DIRECTIONAL-SANITY/REVIEW-HORIZON rules, plus a new TIME RESOLUTION block instructing the model to derive `expiresAt` as the earliest of its relative review horizon and the next relevant HIGH-impact macro event visible in the Payload's `selectedNewsEvents` — never two competing deadlines, exactly one resolved timestamp.
- **`evaluateScenarioExecutionReadiness(scenario, decision, referenceTime)`** (new, `prototype/index.html`) — one pure function, the sole readiness authority, reused verbatim by: the Parser's `PRIMARY` cross-rule (rejects a freshly-submitted non-ready response), Strategy's "手动执行"/"触发时提醒" buttons (disabled + reasons shown when not ready), the Execution page guard (a `DRAFT` draft whose scenario isn't ready is blocked outright — "该方案不满足执行就绪要求" — never an editable prose field standing in as an order parameter; an already-`CONFIRMED` draft still renders as immutable history), and TESTNET submission eligibility (`checkOrderSubmissionEligibility()`, now requires readiness in addition to every 3.5D-B check). This is also what makes a historical `CONDITIONAL`-entry record correctly un-executable going forward without any record migration — the stored record is untouched, only new action on it is gated by today's rules.
- **Trigger Monitor**: already used `reviewHorizon.expiresAt` directly for `NOTIFY` expiry since Sprint 3.4B — confirmed unchanged by this sprint's regression, no code touched.
- **Strategy UI**: every Scenario card now shows an explicit "有效至" expiry row (viewer-local time + UTC offset, never a bare zone-less timestamp) and a "缺少可执行数值参数" indicator + reasons when not ready.

**Local verification**: `npm run typecheck` + full frontend syntax check both passed. **Zero paid AI requests were made during implementation** — every test uses synthetic fixtures; no TESTNET order was submitted (frontend/Parser-only sprint, no backend/adapter code touched). New functional tests — **32/32 Parser+readiness (numeric scenario acceptance/rejection, time-resolution/macro-precedence, directional sanity) + 20/20 UI (Strategy numeric display, Execution readiness block, TESTNET eligibility readiness) = 52/52 checks passed**. Full regression re-run clean: old Parser suite 27/27 (12 fixtures intentionally updated — CONDITIONAL/description-only/no-expiresAt fixtures no longer represent valid PRIMARY plans, one case's expectation flipped from PASS to REJECT), Strategy render tests, Execution 3.4A suite (2 fixtures updated + 1 new blocking-behavior case added), Trigger Monitor 3.4B suite (3 fixtures updated), Order/Trade 3.4C suite (2 assertions updated — CONDITIONAL now blocks instead of showing an order-type picker), Risk Guard 71/71 (untouched), Exchange Connections + Order Lifecycle + TESTNET submission suites (3.5B/3.5C/3.5C.1/3.5C.2/3.5D-A/3.5D-B, all untouched code paths, 2 fixtures needed an `expiresAt`/readiness-dependency update), backend exchange suites (untouched, all pass).
**SPEC sync**: `AI_RESPONSE_SPEC.md` → 2.2.0, `EXECUTION_SPEC.md` → 1.3.0 (new "Scenario Execution Readiness Gate" section). No new SPEC created.
**Real credential/paid-AI check**: no paid AI request, no real Binance request of any kind occurred this sprint.
**3.5D-B.1's then-next milestone** (the one manually-approved TESTNET entry order, and `Position`) is addressed below.

**Previous Milestone (3.5D-C)**: `Position` is now formally defined and implemented as `RealPosition` (`qa_positions`), a mirror synced only from real exchange data (Core Truth Rule: "exchange truth wins" — never from AI, `ScenarioPlan`, `ExecutionDraft`, or local Order status alone).
- `syncPositionsFromExchange(connectionId)` reuses the existing `GET /api/exchange/connections/:id/account` route — no new backend endpoint. Creates/updates local Positions from every non-zero exchange position (exchange overwrites every mutable field on each sync); a previously-`OPEN` local Position missing from a fresh read is exchange-confirmed zero exposure and moves to `CLOSED` (history kept, never deleted).
- Identity key: `(exchangeConnectionId, symbol, positionSide)` — `positionSide` is Binance's own raw hedge-mode label, already present on every `ExchangePositionSnapshot`, so hedge-mode safety needed no separate account-mode lookup.
- `linkPositionToTrade()` links a Position to a `RealTrade` only when exactly one candidate matches the full chain (symbol/direction/connection + a real filled ENTRY order); zero or multiple candidates leave it unlinked rather than guessed. Idempotent.
- Trade Detail now shows a truthful three-way state (no fill / filled-and-waiting-for-sync / linked real Position), reuses the existing `renderPositionCard()` verbatim, shows real `unrealizedPnl` in the header, warns prominently when a real `OPEN` Position has no real stop-loss order, and always renders AI-planned protection in a separate labeled block, never merged with real exchange protection.
- Manual-only combined sync actions: "同步交易状态" (Trade Detail) and "同步本地镜像" (Settings account page) — no automatic polling, consistent with every other sync path in this app.
- No TESTNET order was submitted this sprint. No new backend route. No cancel/amend/leverage/margin-mode mutation anywhere.

**Local verification (3.5D-C)**: backend `tsc --noEmit` clean (no backend file was touched this sprint). Frontend syntax check passed. 56/56 new tests passed in a dedicated suite covering Position creation, hedge-mode identity, update (exchange-wins), close (zero-exposure), connection isolation, Trade linking (including ambiguous / zero-candidate / connection-mismatch / zero-fill cases), Trade Detail UI truth boundaries, and the combined sync action. Full regression re-run clean across every prior suite; 5 pre-existing assertions across 3 files were disclosed-intentionally updated because they asserted the pre-3.5D-C rule "`qa_positions`/Position Sync must not exist yet," which this sprint intentionally supersedes — reworded to match the new truthful UI text, nothing silently weakened. Static checks confirm exactly one write route still exists app-wide (`POST /api/exchange/connections/:id/orders`, untouched), no cancel/amend/leverage functions anywhere, and the new Position Sync code makes exactly two `fetch()` calls, both pre-existing GET routes, zero POSTs.
**SPEC sync (3.5D-C)**: `EXECUTION_SPEC.md` → 1.4.0 (new "Real Position Sync" section; Position boundary section and Non-Goals updated to match). No new SPEC created.
**Real credential/paid-AI check (3.5D-C)**: no paid AI request, no real Binance request of any kind occurred during implementation.
**3.5D-C's real-verification question was resolved directly with you** (order identified by tradeId, one read-only order+position sync performed) before this sprint began — not repeated here.

**Previous Milestone (3.5D-D)**: implemented the app's SECOND real exchange write — TESTNET-only manual cancellation of an already-submitted entry order (`DELETE /fapi/v1/order`).
- **Eligibility**: only `SUBMITTED`/`PARTIALLY_FILLED` with a real `exchangeOrderId` — `DRAFT`/`FILLED`/`CANCELLED`/`REJECTED`/`EXPIRED` or a missing `exchangeOrderId` are all rejected before any network call.
- **Mandatory fresh pre-cancel check**: reuses the existing `syncRealOrderFromExchange()` verbatim before ever showing a confirmation modal — if the exchange now reports `FILLED`, no `DELETE` is sent, local state syncs to `FILLED`, and a Position Sync runs immediately; if already terminal (`CANCELED`/`REJECTED`/`EXPIRED`), no `DELETE` is sent either. This is also what makes repeated cancel clicks idempotent — a stale click can never send a second `DELETE` once exchange truth is terminal.
- **Confirmation modal**: shows TESTNET, symbol, side, order type, `exchangeOrderId`, current status, committed/executed/remaining quantity; a `PARTIALLY_FILLED` order gets an explicit "撤单只会取消剩余未成交数量，已成交部分不会被撤销" warning. Never a one-click cancel.
- **Backend**: new `DELETE /api/exchange/connections/:id/orders/:exchangeOrderId?symbol=` route — the app's second write route. `binanceFuturesAdapter.cancelBinanceFuturesTestnetOrder()` reuses the existing `toBinanceFuturesSymbol()`/`normalizeExchangeOrder()`/error-classification helpers, TESTNET-guarded at three independent layers (adapter, `exchangeService`, frontend), never auto-retried on timeout/network ambiguity (`cancellation_status_unknown`), never auto-retried on a deterministic rejection either (Binance `-2011` → `order_not_cancellable`).
- **Trade lifecycle**: reuses `propagateEntryOrderStatusToTrade()` verbatim — zero-fill cancellation moves `RealTrade.status → CANCELLED`; any partial fill leaves the Trade untouched (real exposure already exists) and triggers an immediate `syncPositionsFromExchange()`. Fill quantity/average price are always preserved on cancel, never reset to zero.
- **Explicit non-automation**: Scenario expiry never auto-cancels a still-live exchange order (Trade Detail shows a passive "策略方案已过期，但交易所订单仍有效。请手动决定是否撤单。" notice instead); a Risk Guard result becoming `BLOCKED` after submission never auto-cancels either — cancellation is a manual, one-click-at-a-time decision at all times.
- No amend/replace, no leverage/margin-mode mutation, no LIVE cancellation, no AUTO, no protective-order submission. No TESTNET order was submitted or cancelled for real this sprint.

**Local verification (3.5D-D)**: backend `tsc --noEmit` clean. Frontend syntax check passed. **101 new tests passed** (31 backend — DELETE payload/symbol normalization/TESTNET guard/ambiguous-failure/deterministic-rejection/response-normalization; 70 frontend — eligibility, fresh pre-cancel branching for all six remote statuses, partial-fill preservation + Position Sync trigger, zero-fill Trade cancellation, ambiguous-failure no-auto-retry + recovery, idempotency, Trade Detail UI states, truth boundaries). Full regression re-run clean across every prior suite (Parser, Strategy, Execution, Trigger Monitor, Order/Trade, Risk Guard 71/71, Exchange Connections, Order Lifecycle, TESTNET submission, Numeric Scenario, Position Sync, connection-isolation/-selection, symbol-normalization) — one pre-existing static assertion (`v3_5c1_timestamp_hotfix_test.ts`'s "no trading write methods" scan) was disclosed-intentionally updated to stop forbidding the substring `cancelOrder`, since cancellation is now a real, deliberately-added capability; `placeOrder`/`amendOrder`/`setLeverage`/`setMarginType`/`transfer(`/`withdraw(` remain forbidden and still pass. Static proof: total real Binance write surface is now exactly `POST /fapi/v1/order` + `DELETE /fapi/v1/order`, confirmed via source scan (zero other write-method names anywhere) and via the new cancellation code section itself making exactly one `DELETE`, zero `POST`, across its whole implementation.
**SPEC sync (3.5D-D)**: `EXECUTION_SPEC.md` → 1.5.0 (new "Real Order Cancellation" section; two now-stale "no cancel route" sentences elsewhere in the doc corrected). No new SPEC created.
**Real credential/paid-AI check (3.5D-D)**: no paid AI request, no real Binance request of any kind (read or write) occurred during implementation.
**3.5D-D's real-verification question is superseded by 3.5D-E below** — not repeated here.

**Current Milestone (3.5D-E)**: closed the last major gap in the real execution loop — after a real fill, the app now automatically establishes real Stop Loss + Take Profit protection, upgrading `ExecutionDraft → RealTrade → Entry Order → Submit Entry` into `Entry → Real Fill/Position → Stop Loss Protection → Take Profit Protection → Protection Verification`.
- **Before Coding research** (disclosed, live-verified 2026-08-10, not assumed from memory): conditional orders (`STOP_MARKET`/`TAKE_PROFIT_MARKET`) create via `POST /fapi/v1/algoOrder` (`algoType=CONDITIONAL`); cancel via `DELETE /fapi/v1/algoOrder`; query single via `GET /fapi/v1/algoOrder` — both accept `algoId` OR `clientAlgoId`, and **neither requires `symbol`** (confirmed against two independent sources, genuinely differing from the plain-order cancel/query shape). The existing read-only `GET /fapi/v1/openAlgoOrders` is unchanged. Cancel Algo Order's real response is `{algoId, clientAlgoId, code, msg}` — modeled as its own honest type, never fabricated as a full order snapshot.
- **Take-profit portions**: `AI_RESPONSE_SPEC.md` has no `portion` field (confirmed before coding — no Parser/Prompt touched this sprint). Execution's take-profit editor now requires a per-target portion (>0 each, sum exactly 100%, never auto-averaged); the full-trade-plan submission gate blocks on an invalid split.
- **Core Truth Rule** (same discipline as Real Position Sync): protection quantity is always `Math.min(realPosition.quantity, entryOrder.filled.quantity)`, never `positionSizeUsdt`, rounded down to the real `LOT_SIZE` step.
- **`reconcileTradeProtection(tradeId)`**: the one reconciliation function — idempotent, creates only what protection is currently missing, never amends or auto-cancels/auto-recreates an existing protection order (a disclosed, deliberate MVP boundary). Powers three call sites: automatic continuation right after a confirmed real fill (MARKET, or a LIMIT that fills immediately), automatic continuation when the user's own manual "同步订单状态"/"同步交易状态" click later discovers a real fill (never a second, separate click), and the Trade Detail "重新建立保护" button (re-runs reconciliation only, never resubmits the entry order).
- **Hedge Mode / Position Side**: never assumed one-way — `RealPosition.positionSide` (already real since 3.5D-C) decides `reduceOnly`-in-one-way vs explicit-`positionSide`-in-Hedge-Mode; Binance's Hedge-Mode `reduceOnly` rejection is refused locally before any network call, never silently dropped.
- **`RealOrder.role`** gains `STOP_LOSS`/`TAKE_PROFIT` (`intent:'CLOSE'`, never confused with `ENTRY`/`OPEN` by existing eligibility checks); `TAKE_PROFIT` orders additionally carry `targetIndex`/`portion`. `RealTrade` gains `protectionStatus` (`NOT_REQUIRED`/`PENDING_ENTRY_FILL`/`PENDING_PROTECTION`/`PROTECTED`/`PARTIALLY_PROTECTED`/`PROTECTION_FAILED`) — all additive, nothing renamed.
- **Duplicate prevention / ambiguous recovery**: identical `pendingClientOrderId`-before-POST discipline as entry submission; a write timeout/network failure is never auto-retried, recoverable only via `GET .../algo-order?clientAlgoId=`.
- **Cancellation interaction**: cancelling a zero-fill entry sets `protectionStatus→NOT_REQUIRED`; cancelling the remaining quantity of a partially-filled entry never removes existing protection (`reconcileTradeProtection()` runs instead, honestly re-confirming coverage).
- **No automation on Scenario expiry or Risk Guard state changes** — passive notices only, exchange truth always wins once a real trade has begun.
- **UI**: Trade Detail's submit button renamed "提交完整交易计划到 TESTNET" with an expanded confirmation modal (entry + stop loss + every take-profit target with portion); a new "保护状态" card shows each real protection order's trigger/portion/status/quantity/`algoId`, with an honest "保护状态未知" state (never a false "no stop loss" claim) when protection-read itself fails.
- No LIVE write, no `AUTO`, no order amendment, no leverage/margin-mode mutation, no trailing-stop/OCO engine. No real TESTNET order or protection order was submitted this sprint.

**Local verification (3.5D-E)**: backend `tsc --noEmit` clean. Frontend syntax check passed. **85 new tests passed** (39 backend — DELETE/POST/GET algoOrder payload correctness, one-way vs Hedge Mode parameter shape, quantity rounding never rounds up, TESTNET hard guard, ambiguous-failure no-auto-retry, real Cancel Algo Order response shape; 46 frontend — Cases A–O from MARKET-fill-full-success through LIVE-hard-reject, covering partial fill, quantity-never-exceeds-position, duplicate prevention, cancellation interaction, hedge mode, protection-read failure, and the portion-validation gate). One real gap was found and fixed BY the tests before any regression run: `reconcileTradeProtection()` was initially missing the same frontend-side TESTNET defense-in-depth check every other write path already has — added, then verified. Full regression clean across every prior suite; 2 pre-existing fixtures (`v3_5d_b_frontend_test.js`, `v3_5d_b1_ui_test.js`) needed a disclosed, intentional update — their take-profit fixtures predated the portion concept and got explicit portions (100% / 50%+50%) added so the new full-plan eligibility gate could evaluate them correctly. Static proof: total real Binance write surface across the whole app is now exactly `POST`/`DELETE /fapi/v1/order` + `POST`/`DELETE /fapi/v1/algoOrder`, confirmed via source scan (no `setLeverage`/`setMarginType`/`amendOrder`/`replaceOrder`/`transfer(`/`withdraw(` anywhere) and via the new Protection code section itself making exactly 1 POST / 0 DELETE across its whole implementation (the backend DELETE-algo-order route exists but is intentionally not yet wired to any UI action this sprint, per the "no auto-cancel/recreate" decision).
**SPEC sync (3.5D-E)**: `EXECUTION_SPEC.md` → 1.6.0 (new "Real Trade Protection" section; several now-stale "protective-order submission unimplemented"/"last write capability" sentences elsewhere in the doc corrected). No new SPEC created.
**Real credential/paid-AI check (3.5D-E)**: no paid AI request, no real Binance request of any kind (read or write) occurred during implementation — the one real network activity this sprint was the public, unauthenticated Binance API documentation research (WebFetch/WebSearch), disclosed above.
**Current Milestone (3.6A)**: adds `PositionAllocation` (multiple local Trades may share one real net Position, attribution never exclusive, `allocationStatus: OK|CONFLICT` computed honestly and never silently resolved — see EXECUTION_SPEC.md's "Position Allocation" section), a new top-level Account/Portfolio page, the app's fifth real write (TESTNET-only leverage change, reachable only via an explicit recovery flow off the existing `leverage_mismatch` submission BLOCK), a real-time SSE update channel (backend-only credentials, REST always remains the reconciliation truth), and a fix for the recurring stale-backend development problem (`npm run dev` via `tsx watch`, plus `startedAt`/`runtimeVersion` on `GET /health`).
- **`PositionAllocation`** (`qa_position_allocations`): `reconcilePositionAllocations(position)` creates one allocation per qualifying Trade (real filled ENTRY quantity only, same Core Truth Rule as Position Sync/Protection), never exclusive like `linkPositionToTrade()` (left unchanged). Idempotent — an allocation's `quantity` is never rewritten once created. `getPositionAllocationSummary()` computes `OK`/`CONFLICT` by comparing the sum against real Position quantity; the block is folded into `renderPositionCard()` itself, so Trade Detail and the new Account page both show it with zero duplicated rendering logic.
- **Account / Portfolio page** (`#/account`): real account overview + every real Position (App/external undifferentiated, each with its allocation breakdown) + every real ordinary open Order (App vs External ownership shown; manual cancel for both — an External cancel carries an extra-strong warning since this app cannot verify true ownership) + every real open Algo/Protection order (App vs External shown, read-only — no cancel UI, preserving 3.5D-E's own decision not to revisit it). Exactly three read-only Position drill-downs (查看详情/查看保护/查看归因); no ADD/REDUCE/CLOSE/REVERSE anywhere.
- **Real Leverage Sync**: `POST /fapi/v1/leverage` (verified live 2026-08-10), TESTNET-only, never invoked automatically — only from an explicit "调整为 Nx 并继续" click on a modal that already showed current-vs-required leverage (extra-strong warning if a real Position already exists for that symbol). Resubmission after a successful change reuses the existing leverage-match gate + Risk Guard rerun as its own read-back verification, rather than a second, diverging check.
- **Real-Time Account Updates**: backend-managed `listenKey` + WebSocket bridge (`pipeline/src/exchange/userDataStreamManager.ts`) relayed to the browser via SSE (`GET .../stream`) — credentials never leave the backend. A stream event only ever triggers a debounced (2s) REST reconciliation; nothing safety-relevant is ever gated on stream data alone. Connection status (已连接/重新连接中/已断开) always shown honestly; manual "立即刷新" remains available regardless.
- **Dev workflow fix**: `npm run dev` (`tsx watch src/server.ts`) added alongside the existing `npm run serve`; `GET /health` now returns `startedAt`/`runtimeVersion` so a stale backend process is detectable without manually comparing `Get-Process` start time against file mtimes — the recurring real problem diagnosed at least three times earlier in this project.
- No Hedge Mode switching, no margin-mode mutation, no automatic CLOSE/REDUCE/REVERSE, no automatic trailing stop/break-even, no LIVE write, no `AUTO`, no portfolio-level optimization — all remain explicitly out of scope, unchanged from every prior sprint's boundary.

**Local verification (3.6A)**: backend `tsc --noEmit` clean. Frontend syntax check passed. **90 new tests passed** (32 backend — leverage change TESTNET guard/invalid-range/ambiguous-vs-deterministic/symbol-normalization, open-orders read normalization, listenKey lifecycle including the no-signature-required request shape, WS URL helper, exchangeService wrapper defense-in-depth, static write-surface scan; 58 frontend — allocation creation/idempotency/OK-vs-CONFLICT/boundary exclusions (wrong connection/direction/zero-fill/closed position), Account page ownership badges + loading/error states, external-order cancellation TESTNET guard + ambiguous/deterministic outcomes, leverage-sync modal content + full change-then-resubmit chain (success, deterministic failure with no resubmission attempted, ambiguous failure with no resubmission attempted), SSE lifecycle (dedup/switch/teardown) and debounced-refresh behavior (burst collapse + genuine eventual fire), static safety proof). Full regression re-run clean across every prior suite this session has accumulated (parser, strategy render, execution 3.4A/3.4B/3.4C, risk guard 71/71, exchange connections, order lifecycle, TESTNET submission/cancellation/protection, numeric scenario, position sync, connection isolation/selection, symbol normalization, timestamp hotfix, position normalization) — zero disclosed-intentional fixture updates were needed this sprint (all changes were additive). One pre-existing script, `v3_5b1_hotfix_live_frontend_check.js`, was NOT re-run as a pass/fail regression gate — it performs real HTTP calls against a running local backend and asserts a *clean/empty* connection store, which is no longer true of this environment's real, intentionally-populated `exchange_connections.json`; this is expected, unrelated to 3.6A, and was never a repeatable regression test. Static proof: total real Binance write surface is now exactly `POST`/`DELETE /fapi/v1/order` + `POST`/`DELETE /fapi/v1/algoOrder` + `POST /fapi/v1/leverage` (five write functions, confirmed via source scan — no `setMarginType`/`amendOrder`/`replaceOrder`/`transfer(`/`withdraw(`/`changePositionMode` function declaration anywhere), plus the read-only `listenKey` lifecycle (POST/PUT/DELETE, account-stream management only, never a trading write).
**SPEC sync (3.6A)**: `EXECUTION_SPEC.md` → 1.7.0 (four new sections: "Position Allocation", "Real Leverage Sync", "Real-Time Account Updates", "Account / Portfolio Page"). No new SPEC created.
**Real credential/paid-AI check (3.6A)**: no paid AI request, no real Binance write of any kind occurred during implementation — the leverage-change/open-orders/listenKey code paths were exercised only against mocked `fetch`/`EventSource` in tests, never a real Binance endpoint (TESTNET or LIVE).
**Current Development Priority**: this sprint's leverage-sync and real-time-update capabilities are implemented and tested but have NOT been verified against a real TESTNET account end-to-end (unlike 3.5D-E.1's protection verification) — awaiting your decision on whether/when to run that verification. `AUTO`, Hedge Mode switching, margin-mode mutation, and automatic CLOSE/REDUCE/REVERSE remain gated on product decisions not yet made.

**Verification note (carried over from Sprint 3.3A, still applicable)**: no browser automation tool (e.g. Playwright, puppeteer-core) is available in this session. End-to-end verification substitutes real HTTP calls / synthetic fixtures / functional render tests — driven by the actual extracted, shipped functions (`prototype/index.html`, via a Node `vm`-sandboxed extraction, not reimplementations) — rather than a literal browser click-through. Disclosed, not silently substituted.

## AI Key Storage (Sprint 3.2C.1 review; extended Sprint 3.3B.2)

`pipeline/data/ai_provider_config.json` (legacy) and, as of Sprint 3.3B.2, `pipeline/data/ai_connections.json` both store API keys in **plaintext on local disk** — same accepted limitation, same reasoning: no encryption or secret manager introduced, this remains a JSON file, not a database. The new connections file is in the exact same `data/` directory, covered by the exact same gitignore rule, and its CRUD API (`connectionStore.ts`/`server.ts`) was built to the same masking contract as the original review below — verified again this sprint:

- **Never exposed to the frontend**: `GET/POST/PUT /api/ai/connections*` all route through `toSafeConnectionView()`, which includes only `apiKeyMasked`/`hasApiKey` — confirmed live against the real backend that a full key never appears in any connections response.
- **Never written to browser `localStorage`**: the new Connections form holds a typed key only in an in-memory draft (`Store.connectionDraftForm`), discarded (not persisted) on cancel/save-success. (The OLD `Store.aiProviderConfigDraft` this note previously referenced is now inert — the old per-provider-card UI that wrote to it was removed this sprint; the dev Prompt Preview modal's still-untouched provider picker never writes an apiKey into it at all.)
- **Never printed in logs**: unchanged — `providerHttp.ts`'s `postJson()` still scrubs every known secret from provider-response text before attaching it to a thrown error, and `modelDiscovery.ts`'s new `/models` endpoint deliberately never surfaces raw response bodies at all (a fixed safe message + HTTP status only), a stricter rule than even the redaction approach, since this endpoint has no legitimate reason to show provider-authored text.
- **Gitignored**: same `data/` rule, confirmed to cover the new file too.
- **No configuration export feature exists** — nothing to review there.

Conclusion unchanged: plaintext-on-disk storage is acceptable for this prototype's threat model (local, single-user, no deployment).

---

# Specifications

Contract documents only — read the one relevant to the current task, not all of them.

- `DATA_SOURCE_SPEC.md` — Dashboard data source architecture (Connector → Normalize → Snapshot).
- `ANALYSIS_PAYLOAD_SPEC.md` — the Analysis Payload contract (v1.1.0; Sprint 3.3B added the optional `additionalEvidence[].parsedContent` field; Sprint 3.3B.2 added a non-version-bumping addendum noting `parsedContent`'s internal `source`/`context` sub-fields).
- `PROMPT_BUILDER_SPEC.md` — Payload → Prompt Record contract.
- `AI_RESPONSE_SPEC.md` — the AI response contract (v2.1.0, Sprint 3.3D-C — additive "Scenario Execution Contract" upgrade on top of Sprint 3.3D-A's breaking v2.0.0 rewrite: `ScenarioPlan` gains optional `triggerStructured`/`invalidation`/`reviewHorizon`; `entry`/`stopLoss`/`takeProfit` completeness is now action-independent, closing the last `WAIT`-incompleteness gap; implemented by the Sprint 3.3D-C Parser and rendered with a minimal Strategy UI adaptation, same sprint).
- `EXECUTION_SPEC.md` — the `ExecutionDraft`/`ExecutionMode`/`TriggerStatus`/`RealOrder`/`RealPosition`/`StrategyOrder` contract (v1.14.0, Sprint 3.7 — freezes `MANUAL`/`NOTIFY`/Trigger Monitor (3.4A/3.4B), the Scenario Execution Readiness Gate (3.5D-B.1), TESTNET-only entry order submission (3.5D-B) and cancellation (3.5D-D), Real Position Sync (3.5D-C), TESTNET-only real Stop Loss/Take Profit protection via Binance's Algo Service (3.5D-E, verified for real in 3.5D-E.1), `PositionAllocation` + TESTNET-only leverage change + real-time SSE account updates + the Account/Portfolio page (3.6A), `PositionAllocation`'s position-lifecycle correctness fix (3.6A.1), Exchange Account Identity + Trading History Reset + Prototype Retirement + Algo Cancel Reachability (3.6A.2), `StrategyOrder` + Trigger Engine + Capability Router + user-authorized `MONITORED_AUTO` execution (3.6B), the fully backend-owned Headless AUTO lifecycle + backend AUTO Risk Policy + Capability Router correctness fix (3.6B.1), External Position Closure Reconciliation (3.6B.2), the Orphaned Protection Reconciliation hotfix (3.6B.2.1), and Autonomous Trading V1 — a scheduled `NO_ACTION`/`WAIT`/`OPEN` AI decision source, TESTNET-only, fully governed by the existing deterministic safety pipeline (3.7). `AUTO REDUCE/CLOSE/REVERSE`, LIVE auto-execution, AI-controlled leverage/SL/TP mutation after a Position is active, Hedge Mode switching, margin-type mutation, and Manual Position Close remain explicit non-goals).
- `docs/ARCHITECTURE_INDEX.md` — concise navigation index (main files/functions per domain area); read before broad codebase changes.
- `docs/BINANCE_CAPABILITY_MATRIX.md` — live-verified Binance USDⓈ-M Futures capability reference backing the Trigger Capability Router's decisions.

---

# Development Workflow

- Small, incremental sprints — each scoped narrowly and explained before implementation.
- Never redesign a frozen module without an explicit, recorded product decision.
- Product decisions update this file (`PROJECT.md`); contract changes update the relevant SPEC document.
- `product-docs/archive/` holds historical/superseded documents — never required reading to understand current state.
- **Backend startup (Sprint 3.6A)**: `npm run dev` (`pipeline/`) runs the backend with `tsx watch`, auto-restarting on any source-file change — use this during active development. `npm run serve` (plain `tsx src/server.ts`, no watch) remains for a one-off/manual run. Both print a real `startedAt` on `GET /health`, alongside `runtimeVersion` — check these against the current time / a recent source edit before trusting real TESTNET behavior; a stale backend process running old code has caused real verification failures at least three times earlier in this project.
